# Service-level x-kedge placement (as shown in the site's compose examples).
services:
  web:
    image: traefik/whoami
    ports: ["80"]
    x-kedge:
      auth:
        google: true
        github: true
