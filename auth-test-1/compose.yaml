# Auth gates only the backend worker route; the front page stays public so the
# logged-out vs logged-in contrast can be demonstrated from one screen.
x-kedge:
  auth:
    paths: ["/whoami", "/whoami-identity"]
    google: true
    github: true
